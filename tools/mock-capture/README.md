# mock-capture

CLI stub per [MOCK_CAPTURE_AGENT_SPEC.md](../../docs/06-stack-evaluation/MOCK_CAPTURE_AGENT_SPEC.md).

```bash
pip install -r requirements.txt
python mock_capture.py --api-url http://localhost:8080
```

Requires `docker compose -f infra/compose.dev.yaml up`.
