# PedagogyX Web (admin boilerplate)

Next.js 15 App Router — school overview shell (M-A widget placeholder).

```bash
cd services/web
npm install
NEXT_PUBLIC_API_URL=http://localhost:8080 npm run dev
```
