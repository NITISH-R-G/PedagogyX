# worker-asr

Redis queue consumer. **Boilerplate** logs jobs; replace `process_job` with faster-whisper (Sprint 03 S03-05).

GPU image variant will extend this Dockerfile with CUDA + `faster-whisper` per ADR-0006.
